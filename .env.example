# SQLite (lokal)
DATABASE_URL="file:./dev.db"

# PostgreSQL (Produktion) — in schema.prisma provider auf "postgresql" ändern
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?schema=public"

# Auth.js (mind. 32 Zeichen in Produktion)
AUTH_SECRET="dev-secret-only-for-local-development-32chars"
NEXTAUTH_URL="http://localhost:3000"
